{
    "name": "cypress-docgen",
    "version": "1.0.0",
    "type": "commonjs",
    "scripts": {
        "build": "tsc",
        "start": "node dist/index.js",
        "generate:doc": "npm run build && npm run start",
        "generate:doc:folder": "npm run build && node dist/folder-docs.js"
    },
    "devDependencies": {
        "@types/node": "^22.14.1",
        "typescript": "^5.3.3"
    }
}